<h1>Comments for <%= @article.title %></h1>

<h2>All Comments</h2>
<% @comments.each do |comment| %>
  <div>
    <strong><%= comment.commenter %>:</strong>
    <p><%= comment.body %></p>
    <%= link_to 'Edit', edit_article_comment_url(@article, comment) %> 
   <a href=<%="/articles/#{@article.id}/comments/#{comment.id}/edit" %>> EditArticle </a>
    <%= link_to "Destroy", article_comment_path(@article, comment), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
  </div>
<% end %>

<h2>Add a Comment:</h2>
<%= render 'form', article: @article, comment: Comment.new %>